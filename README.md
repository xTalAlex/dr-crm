# Dr. CRM

## Setup

```bash
npm install
npm run dev
```

## Database

Per sincronizzare lo schema Prisma con un DB Supabase (cancella e ricrea le tabelle):

```bash
npx prisma db push --force-reset
```
